#include<stdio.h>
int main()
{
    int i=1,num;
    long int fact=1;
    printf("enter the number=");
    scanf("%d",&num);
    for(i=1;i<=num;i++)
    fact=fact*i;
    printf("factorial of %d is:%1d",num,fact);
    return 0;
}
